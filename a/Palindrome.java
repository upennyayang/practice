// 1) string palindrone / use js to implement a set with js object
// 2) given a list of integers, return the pairs that add up to a given number / data modeling的问题,几个表外键相连,让写比较复杂的query
// 3) data modeling 给需求,设计ER图,和面试官讨论各种design choice和trade off / 设计模式: inherit VS composition, factory pattern, singleton pattern. 1point3acres.com/bbs
// 4) design the data model for twitter / understanding about JVM interal, how,where,when are classes loaded
// 5) implement BigSet / multi-list merge / linked list remove node

//  given a list of pairs, pair.first 表示parent, pair.second表示child,
// reconstruct the tree, return the root node.
// 2. auto completion - design the service
// 3. design a service, accept stream of events, each event has a type and
// timestamp, need to support the query of top k most frequent types in a query
// specified [start, end] time range.
// 4. closest number to target in BST
// 5. validate soduku / solve soduku, and optimizations
// 6. 给一个json object，给一个wildcard path with ‘?’ as arbitrary name，比如
// a.?.b 找到所有符合path的objects